# Summarize The Plan Gate

Summarize the cleared gate for the operator and for stage 2: the verdict,
each binding constraint and where the committed plan discharges it, and
the committed plan's step-table shape (slice count, move-only versus edit
split, total estimated size). If the gate was refused, summarize why and
state that the campaign stops here. Include the exact lowercase `author:`
byline near the top of the artifact.
