<!-- Generated by `striatum init --with-ddd-layout` (RFC 0021).
     Edit freely; this file is not regenerated. -->

# DECISION_LOG

## How to read this file

Each row is a decision the project has made. The four columns
hold *receipts*: a one- or two-sentence prose cell each. Detail
belongs in the linked RFC and any associated build handoff,
not in the row itself.

The cell budget exists so the log stays a navigable receipt and
does not grow back into duplicated SPEC content. Newer rows are
expected to honor the budget; older rows may be grandfathered.

## How to add a row

1. Pick the next sequential ID (`D###`).
2. Write the four cells:
   - **Decision** — what we decided. Imperative, present tense.
   - **Context** — why we needed to decide. The tension we
     were resolving.
   - **Consequence** — what changes as a result. Cite the RFC
     and any code/test entry points.
   - **Revisit** — the conditions that would justify reopening
     this decision.
3. Cite the RFC and (when applicable) a build handoff or PR.

## Decisions

| ID | Status | Decision | Context | Consequence | Revisit |
| --- | --- | --- | --- | --- | --- |
| D001 | accepted | Adopt the striatum context-hygiene practices: an `AGENTS.md` entry-point reading list, vendored `docs/CONTEXT_HYGIENE.md`, a real RFC index, and labeled negative space for scaffold-only docs. | Repo had `DDD.md` / `SPEC.md` / `UBIQUITOUS_LANGUAGE.md` / `DECISION_LOG.md` skeletons but no entry-point file, and the RFC index and this log were both TODO placeholders. New agents had to triage the doc tree before doing useful work. | `AGENTS.md` (root) is the canonical reading list; `docs/CONTEXT_HYGIENE.md` is vendored byte-identical from striatum so upstream updates can be diffed; `docs/rfcs/README.md` index now lists 0001–0008. | If `AGENTS.md` outgrows the 5–8 file budget, or if the entry-point pattern is replaced by something the wider agent ecosystem converges on. |
| D002 | accepted | Expand the PRD scope to absorb RFCs 0005–0008. Hydrostatics, analytical resistance estimation (Michell + ITTC), a portable browser frontend, and a headless CLI are *in scope*. The out-of-scope list is reduced to four genuine domain boundaries (full-boat solid, structural, outfitting, non-kayak hulls). | The original PRD was drafted before the project's direction expanded toward generative CFD evaluation. Its "out of scope" list called out hydrostatics, CFD, and a web interface as excluded — directly contradicted by RFCs 0005–0008. Leaving that contradiction in place forces every future contributor (human or agent) to read the PRD with a footnote, and risks re-litigation of decisions the RFC trail has already settled. | `docs/PRD.md` rewritten: audience and in-scope sections describe the full pipeline (geometry + hydrostatics + resistance + desktop + web); out-of-scope list restricted to true non-goals; success criteria added for hydrostatic accuracy, resistance fidelity, and URL-shareable hulls. `AGENTS.md` no longer needs a "where the docs and the RFCs disagree" section. | If a future RFC moves a currently-in-scope item back out (e.g., resistance estimation proves unsalvageably inaccurate at the live tier), or if the project narrows its audience back to "geometry only." |
| D003 | accepted | Adopt a profile-scoped solver-readiness contract: first add an RFC 0040 readiness report, then harden volume-mesh diagnostics, structured blockers/warnings, hashes, boundary metadata, and generated-body matrix coverage before production handoff. | The project needed to decide whether generated bodies, fixture volume meshes, or ordinary packages could move toward solver readiness. The panel unanimously rejected universal thresholds, direct generated-body promotion, and a surface-only profile inside watertight readiness. | Current `cfd_ready` remains only the narrow fixture-backed `watertight_solid_resistance_v1` evidence path. Production readiness work proceeds through report, schema, body-hardening, package-gate, and dispatch-gate slices; no solver success or validated output claim changes. | If a production mesher or solver profile is selected with accepted diagnostic thresholds, or if a future RFC defines a separate physically coherent surface-only solver profile. |
| D004 | accepted | Select OpenFOAM.com OpenFOAM-v2512 `interFoam` as the first real external solver target, under local profile `openfoam-v2512-interfoam-local`, required mesh profile `watertight_solid_resistance_v1`, readiness `cfd_ready`, case template `openfoam-v2512-interfoam-dtchull-v1`, and raw `forces` parser scope. | RFC 0041 was blocked on a named solver, mesh gate, template, parser, platform note, and CI strategy. The panel unanimously found OpenFOAM v2512's maintained DTCHull/free-surface path more relevant for first kayak resistance work than SU2 or an open-surface smoke adapter. | Implementation may add profile metadata, dependency detection, deterministic case rendering, unavailable/failed states, and fake-command/fixture parser tests. No real OpenFOAM `succeeded` path is authorized until matching RFC 0040/RFC 0023 OpenFOAM-readable volume-mesh evidence exists. | If RFC 0040 lands a successor mesh profile name, if OpenFOAM template evidence proves unsuitable, or if an SU2 or surface-only path gets stronger accepted evidence. |
| D005 | accepted | Require a resistance-source review packet and source-use mapping tests before any measured source is promoted. Preserve the five runtime `SourceUse` values and keep `rejected` as a review outcome only; no current source is promoted to validation or calibration fixture by this decision. | The current registry has candidate and citation sources with mixed rights, extraction, uncertainty, and hull-envelope evidence. The panel unanimously rejected immediate promotion and also rejected waiting for a perfect calibration source before source-review infrastructure exists. | Batch F starts with review-packet fields, source-use mapping tests, and one candidate review without automatic promotion. Edinburgh may be a later validation-only workflow after extraction/attribution metadata; calibration fixture promotion requires a measured kayak-envelope source with accepted review. | If a source review accepts measured rows with rights, units, extraction, uncertainty, fixture ID/version, and a declared validation or calibration envelope. |
| D006 | accepted | Preserve the current no-promotion gate for calibrated resistance. Resistance output remains `uncalibrated_comparative` until a later accepted-fit workflow has an accepted kayak-envelope calibration fixture, immutable model version, fit metrics/residuals, validity envelope, and a passing `claim_allows_calibrated_prediction` plus envelope check. | The panel unanimously found that no current source supports calibrated wording, numeric fit thresholds, calibrated comparative-ranking wording, or design-fitness claims. The PRD's 25% over Fn 0.25-0.50 criterion remains only an upper-bound roadmap target. | RFC 0042 source review and fixture promotion remain prerequisites to any fit workflow. Validation fixtures cannot remove warnings or satisfy calibration IDs, and `claim_allows_final_design_fitness` remains separate and unsatisfied. | If RFC 0042 accepts a kayak-envelope calibration fixture and a later accepted-fit workflow defines source-aware thresholds, model versioning, and envelope membership. |
| D007 | accepted | Adopt fixed-trim generated-body v1 as the high-angle `GZ` model design: use `generated_hull_plus_deck_closed_body_v1` after RFC 0024 diagnostics pass, a default 0-90 degree grid by 5 degrees, hull-fixed passive CG, fixed upright trim, per-heel sinkage solve, clipping/capping diagnostics, per-heel status/residuals, grid-bounded summaries, and sealed-body/flooding warnings. | The track was blocked because the body profile, heel grid, trim policy, CG convention, waterline clipping, residuals, and flooding assumptions were unresolved. The panel unanimously chose this v1 comparator over continuing indefinite unavailability, free-trim v1, or flooding/active-paddler models. | Real generated-kayak high-angle values remain unavailable until the v1 model and tests land behind RFC 0024 gates. The future implementation must label results as unvalidated hydrostatic comparison curves, not safety, seaworthiness, capsize, final design-fitness, or validation claims. | If the sealed full-deck body proves misleading at high heel, if free-trim per-heel solving is accepted as a successor mode, or if cockpit/flooding/active-paddler geometry is added by RFC. |
| D008 | accepted | Pursue browser hosting only as a narrow server-backed exploratory public demo using the existing Trame app via `kayakgen serve` or the repo Docker path, with an operator owner, cost/capacity limits, hosted smoke evidence, bounded persistence, and explicit no-SLA/no-hosted-CFD/no-calibration/no-final-claim wording. | The browser-hosting panel split 2-1: two lanes selected the narrow public demo posture, while one lane dissented until an owner and budget exist. All lanes rejected static/Pyodide as a drop-in path and rejected production hosted app or hosted CFD scope. | A public URL is not delivered by this decision. The next hosted-demo workflow is operationally gated on owner, budget/cap, deployment revision, environment, persistence, smoke, and cleanup evidence; static rewrite and production hosting require separate RFCs. | If no operator owner or budget/cap is available, if a static/backend-free runtime is explicitly selected by RFC, or if production hosting requirements are introduced. |
| D009 | accepted | Make the Trame web workspace the primary UI composition and browser-acceptance target for new user-facing workflows, while keeping the PyQt desktop GUI as a supported local surface for launch, implemented sliders, 3D preview, STL export, and no-claim/status maintenance. | The panel unanimously rejected a full native desktop rewrite and desktop deprecation. The project needed a definition of parity that avoids duplicated UI ownership while preserving the documented desktop workflow. | Parity means shared core data/read models, claim boundaries, and implemented controls where surfaced, not pixel or widget parity. A thin desktop shell remains a later packaging/embedding workflow only if user or operator need is recorded. | If desktop launch semantics become a recorded user need, if desktop usage requires new workflow support, or if a future RFC changes the desktop-support commitment. |
| D010 | accepted | Adopt the conservative sweep/comparison admissibility policy: treat RFC 0009 as a landed/partial sweep-run-record slice, keep default Pareto objectives limited to `GM0_m`, `displacement_error_kg`, and `mesh_problem_count` when present, and allow raw resistance only as an explicit exploratory objective with warnings. | The panel unanimously found the sweep runner user-facing despite RFC 0009's stale proposed status, and rejected raw resistance, raw CFD, advisory warning counts, unavailable high-angle stability, and scalar `design_fitness` as defaults. | Optimizer/search work remains blocked until RFC 0009 status is reconciled, an objective metadata registry exists, and claim-gated promotion rules are enforced. Failed/skipped candidates stay visible but frontier-ineligible, and no current metric is final design fitness. | If calibrated resistance, validated CFD, real high-angle stability, or `validated_design_fitness` gates land with accepted-use provenance and explicit objective metadata. |
| D011 | accepted | Select OpenFOAM.com OpenFOAM-v2512 `snappyHexMesh` as the first production volume-mesher candidate under a profile such as `openfoam-v2512-snappyhexmesh-watertight-v1`, but only as a deterministic OpenFOAM-readable evidence harness over `generated_hull_plus_deck_closed_body_v1`. | Workflow 0051 left real OpenFOAM work blocked on production volume-mesh evidence, and the workflow 0052 panel unanimously chose the OpenFOAM-native DTCHull-style path over more fixture-only work, Gmsh-first conversion, or cfMesh/CF-MESH+ selection. | RFC 0040 follow-up may render meshing cases, capture `checkMesh`, patch, artifact-hash, and dispatch evidence, and use fake-command fixtures in CI. Ordinary generated packages, real OpenFOAM `succeeded` records, calibrated CFD, final prediction, design fitness, and broad `cfd_ready` promotion remain unauthorized. | If `snappyHexMesh` cannot produce repeatable acceptable evidence over the generated-body matrix, if a later solver-neutral mesher decision wins stronger evidence, or if the selected OpenFOAM profile changes. |
| D012 | accepted | Keep `openfoam-v2512-interfoam-local` unable to return `succeeded` until one run record binds accepted OpenFOAM-readable `watertight_solid_resistance_v1` / `cfd_ready` volume-mesh evidence, OpenFOAM.com v2512 provenance, a deterministic v2512 case smoke, a v2512-correct `force.dat` parser, and raw-unvalidated payloads. | The panel unanimously found that command success or parser-readable fake output is insufficient; current parser semantics and missing OpenFOAM-readable mesh evidence would make a success state misleading. | The adapter should keep `solver_success_blocked` until all mesh, case, solver-provenance, parser, and no-claims gates pass. Future success means only local solver execution plus raw artifact parsing, not validation, calibration, final prediction, design fitness, or readiness for other hulls. | If RFC 0040 lands a different OpenFOAM-readable mesh evidence profile, if the v2512 parser/case-template evidence changes, or if a separate non-production fixture-success profile is explicitly accepted. |
| D013 | accepted | Select the University of Edinburgh DataShare Pacific-canoe hydrodynamics dataset, DOI `10.7488/ds/3785`, for the first full RFC 0042 resistance source-review packet, capped at `validation_fixture` as the only possible positive outcome. | The panel unanimously found Edinburgh to be the only current measured source with reusable rights, files, CAD, and checksums ready for reproducible review, while kayak-specific K1 sources remain rights-blocked and sea-kayak historical sources remain model-derived or unrecovered. | The next source workflow should complete extraction, unit, uncertainty, attribution, checksum, and fixture metadata for Edinburgh while preserving `outside_sea_kayak_calibration_envelope` as a calibration blocker. No current source is selected for `calibration_fixture`, and resistance output remains `uncalibrated_comparative`. | If a kayak-envelope measured source with reusable rights, extraction, uncertainty, validity envelope, and fixture metadata is accepted, or if Edinburgh review evidence fails validation-fixture requirements. |
| D014 | accepted | Surface fixed-trim generated-body v1 high-angle `GZ` only by a staged, explicit, opt-in path: CLI JSON first, then opt-in sweep artifacts, then display-only comparison and web read models, with desktop minimal/supporting and no objective promotion by default. | D007 selected the v1 model and workflow 0051 made its typed evaluator payload round-trip, but current product surfaces still default to unavailable high-angle output and the panel unanimously rejected broad live/default surfacing. | Any product surfacing must require generated-body and per-heel gates, provenance, body/load/trim warnings, `unvalidated_hydrostatic_comparison` semantics, and no safety, seaworthiness, capsize, ISO, validation, solver-readiness, final-prediction, or design-fitness wording. Default `kayakgen stability`, sweep summaries, frontiers, and default objectives remain unchanged until a later implementation lands the opt-in path. | If measured validation, free-trim solving, cockpit/flooding/active-paddler modeling, or a new objective-admissibility decision changes the allowed interpretation of high-angle output. |
| D015 | accepted | Keep public browser operation deferred until operator and budget evidence exists; when those gates exist, the authorized demo shape is one fixed-size managed container running the existing `kayakgen serve --host 0.0.0.0 --port 8080` or repo Docker path. | Workflow 0052 found no current operator owner, budget/cap, deployed revision, hosted smoke, bounded persistence policy, cleanup receipt, or public no-claims wording, even though D008 had accepted the narrow server-backed exploratory posture. | A successor workflow must record owner/backup, provider/account/region/tier, budget cap or hard limit, deployed SHA, hosted smoke, persistence/cleanup evidence, and no-SLA/no-hosted-CFD/no-calibration/no-final-claim wording before any public URL is accepted. Static/Pyodide, production hosting, accounts, quotas, and hosted CFD require separate decisions. | If an operator supplies the required budget, deployment, smoke, persistence, and cleanup receipts, or if a static/backend-free runtime is explicitly selected by RFC. |
| D016 | accepted | Schedule the RFC 0009 `pending` candidate lifecycle state as the next sweep/search delta: add pending records and counts, explicit transition/resume policy, and visible-but-frontier-ineligible pending candidates. | Workflow 0051 landed objective metadata, while RFC 0009 still has the `pending` lifecycle and sweep-side STL deltas open. The panel unanimously chose lifecycle auditability before binary STL artifacts or active optimizer/search behavior. | The next sweep workflow should keep the JSON contract additive, requeue or re-evaluate pending records on resume, preserve pending rows in `run.json`, `summary.csv`, and comparison reports, and forbid optimizer loops, parallel worker queues, sweep-side STL emission, calibrated resistance, real CFD success, high-angle surfacing, and new design-fitness semantics. | If sweep-side STL artifacts become more urgent after pending lands, or if a later search-spec workflow records algorithm, seed, budget, bounds, constraints, and claim-gated objective metadata. |
| D017 | accepted | Retain `_state_matches_preset_seed` after the RFC 0036 proof attempt: the same-seed listener branch is reachable via the Trame state-driven `_on_hull_param_change` event sequence, and removal would force a preset-to-custom flip when Trame re-fires the listener with state still equal to the preset seed snapshot. | Workflow 0047 left RFC 0036 as a residual decision: prove the branch via browser automation or remove it as dead code. Playwright is not currently authorized as a default-suite dependency, so the proof had to come from the Trame state path itself. | RFC 0036 lands as `landed retain-with-trame-state-proof`. Two new tests in `tests/test_web_layout.py` drive the same-seed sequence via `_apply_class_preset` → bounds widening → `_on_hull_param_change()` without calling the private helper, plus a counter-proof for the custom-flip branch. The helper remains in `kayakgen/ui/web/app.py`. | If Trame removes the post-apply state broadcast, if Playwright becomes part of the default suite, or if a successor preset model removes the seed-snapshot concept. |
| D018 | accepted | Adopt the source-extractor stub pattern for RFC 0042 contracts, with the explicit follow-up that as soon as the upstream data is reachable the stub is replaced and the source-review packet binds the checksum. The 2026-05-15 cowboy session executed both halves: download → vendor → real extractor → checksum-bound packet. | The Edinburgh DataShare workbook is operator-supplied; the packet validators, checksum-binding rules, and source-use gates could be locked before the data landed, but the on-disk implementation needs the workbook to validate the row schema. | `kayakgen/eval/calibration/extractors/edinburgh_datashare_pacific_canoe.py` now parses the vendored `FixedSink_and_TrimDataAnalysis20221114V15_IMV.xlsx` (sheet `Averaged Data`, header row 12, data from row 14, columns Day/Model/Test/Yaw/Speed/Stbd Drag/Port Drag/FWD Side/AFT Side/Heave/Pitch/Velocity/Time/Comment); the source-review packet binds workbook SHA-256 `dffbd5d4…f06c8ea`. `outside_sea_kayak_calibration_envelope` remains the sole non-promotion reason; promotion to `calibration_fixture` still requires a separate accepted-fit workflow under decision D006. | If the workbook gains per-row uncertainty rows, if a different acquisition path is selected, or if a kayak-envelope-compatible measured source replaces Edinburgh for calibration work. |
| D019 | accepted | Land RFC 0043 stage 1 as `kayakgen stability --high-angle-gz` (optional `--heel-grid-deg`), keeping the default stability JSON byte-equal and surfacing the unvalidated hydrostatic comparison only behind the explicit opt-in. | D014 selected the staged surfacing order. The cowboy 2026-05-15 session landed stage 1 first so the rest of the staged path (sweep evaluator, display-only comparison, desktop) can be wired against a stable JSON shape. | Stage 1 record lives at `kayakgen/cli/high_angle_gz.py`; `tests/test_stability_cli_high_angle.py` pins byte-equality of the default path, the surface warnings, the grid-validation gate, and the synthetic-body `available: false` branch. Sweep, comparison, web, and desktop surfaces remain unchanged. | If the heeled-integration model is revised, if Pareto admissibility opens for high-angle GZ, or if measured validation lands and the result semantics change. |
| D020 | accepted | Treat RFC 0040 stage 2 (`snappyHexMesh` evidence harness) as a contract-only landing: ship the case-template lock, dictionary scaffolds, evidence record, and partial-evidence-safe translator without invoking the OpenFOAM binary. Real `snappyHexMesh` execution remains a later workflow. | The 2026-05-15 cowboy session had no authorized OpenFOAM binary, but the harness contract (dictionary hashes, patch metadata, `CheckMeshSummary`, artifact checksums, provenance probe) could be locked deterministically against the existing generated-body record. Waiting for the binary would leave the contract untested. | `kayakgen/eval/snappy_hex_mesh.py` exposes `SnappyHexMeshEvidence` and the deterministic dict scaffolds; `snappy_hex_mesh_volume_mesh_diagnostic` returns `None` for partial evidence and only delegates into the existing `cfd_ready` gate when every gate binds. `tests/test_snappy_hex_mesh_harness.py` pins all rejection paths and the happy translation. No new `cfd_ready` promotion path was created. | If a real `snappyHexMesh` binary lands and the harness needs to execute the binary against generated bodies, or if a non-OpenFOAM mesher decision wins stronger evidence. |
| D036 | accepted | Land RFC 0055 single-file design-report export as an opt-in `kayakgen design-report` CLI under a new `[report]` extras group (jinja2 + optional weasyprint). 10-section self-contained HTML report (header / parameters / rendered views / hydrostatics / stability / resistance / mesh-readiness / optional comparison / artifact refs / claim-state explanations) with embedded base64 PNG preview and a forbidden-copy scan that raises `ReportForbiddenCopyError` rather than write the file. | The cowboy 2026-05-17 "drive all proposed RFCs to completion" wave implemented RFC 0055 with these defaults pre-resolved (jinja2 templates over hand-rolled strings; weasyprint over wkhtmltopdf; embedded PNG over external link) so the report is one shareable file. | `kayakgen/services/design_report.py` + the `design_report_templates/report.html.j2` template; new `[report]` extras; `FORBIDDEN_COPY_TOKENS` + `FORBIDDEN_COPY_SCRUB_TOKENS` named constants in the renderer. +8 tests. Default suite unchanged; jinja2 absence ⇒ test skip. | If a different rendering engine wins stronger evidence, if hosted publishing is reopened (currently D023-deferred), or if a future RFC widens the forbidden-copy scan to additional tokens. |
| D037 | accepted | Land RFC 0057 stage 4 Generate-panel UI polish per the 12-question operator interview captured in `docs/workflows/0054-rfc-0057-stage-4-ui-polish/STAGE_4_DECISIONS.md`. Six new modules ship: a form-builder primary input (with raw-JSON escape hatch, live claim-admissibility filtering, CFD-in-loop opt-in row, soft 4-job-advisory, base-hull pre-fill); 2D scatter + sortable table frontier with objective-pair selector + colour-mapped third axis; 1 s/10 s auto-poll listener pausing while the tab is hidden; one-click fork-with-new-seed button + route + manager primitive; `$HOME`/`<jobs_root>` log redaction; and `kayakgen serve` defaults flipped to the subprocess manager with `--jobs-in-process` as the explicit opt-in. | All twelve decisions were operator-affirmed in the 2026-05-18 interview; the subprocess-default flip and the fork-with-seed shipping in this stage deviated from the original "Recommended" entries. Workflow 0054 was scaffolded but blocked by `striatum supervise send --packet-id` rejecting every claim-next identifier (filed as halbritt/striatum#24), so execution proceeded cowboy-mode on `striatum/0054-rfc-0057-stage-4-ui-polish` under the existing kayak-gen authorisation. | New modules: `kayakgen/ui/web/generate_spec_form.py`, `generate_frontier_view.py`, `generate_state_listener.py`, `generate_fork_button.py`; `kayakgen/services/generative_jobs_fork.py`; redaction helper + log-payload route through `kayakgen/services/generative_jobs.py`; `--jobs-in-process` flag in `kayakgen/cli/main.py`. +49 focused tests across six new test modules. Full repo suite still green; forbidden-copy + ui-theme orphan scans both passing. | If the form-builder schema needs to track a future SweepSpec/SearchSpec version, if the 2D scatter limit becomes a friction point, if the subprocess-default surface gets in the way of a debugging workflow, or if a measured-stability dataset arrives and CFD-in-loop graduates from opt-in to first-class. |
| D038 | accepted | Land RFC 0058 stage 1 stability-calibration acceptance schemas only: `FixtureRef`, `HullFamilyScope`, `StabilityFitMetrics`, `ReviewerSignature`, `StabilityFitRecord`, and `StabilityFixturePromotionPacket` under `kayakgen/eval/stability/accepted_fit.py`. | RFC 0056 supplied measured-stability fixture schemas, but RFC 0043 still needs a separate accepted-fit discipline before any analytical high-angle `GZ` claim label or CFD-in-loop status can graduate. Stage 1 pins the data contract without changing runtime behavior. | `tests/test_stability_accepted_fit.py` covers canonical JSON round-trip, default fit-threshold refusal, strict-check bypass warnings, accepted-verdict metadata, promotion-packet gates, SHA-256 shape, and non-empty design-hash envelope. No fixture or fit is promoted; `resolve_analytical_claim_label`, `cfd_in_loop_evaluator_status`, and `kayakgen stability` remain stage-2/3 scope. | If measured rig data arrives for D007/D014, if the default thresholds need adjustment after pilot-run uncertainty evidence, or if the stage-2 claim/generative-job contracts require schema fields beyond the stage-1 records. |
| D039 | accepted | Land RFC 0058 stages 2 and 3 with persistent opt-out winning over CFD-in-loop graduation, and defer the formal `fit_kind` field on `StabilityFitRecord` to a successor RFC. | The Generate panel needed deterministic default behavior before any accepted stability fit exists, while still naming the future graduation path. A structural `.kind` discriminator is enough for the stage-2 helper without changing the stage-1 schema. | `resolve_analytical_claim_label` and `cfd_in_loop_evaluator_status` now define the contracts; `kayakgen stability` provides schema-only fixture/fit commands; the Generate panel uses the contracts for frontier colour and acknowledgement visibility. Defaults stay byte-stable, no fixture or fit is promoted, and Stage 4 remains gated on D007/D014 physical rig data. | If the successor fit-kind RFC lands, if RFC 0046's persistent-setting API changes, or if a real measured stability fixture and accepted CFD-in-loop fit arrive for the first promotion workflow. |
| D040 | accepted | Preserve the pre-existing `kayakgen stability <hull>` top-level command as a hidden `kayakgen stability legacy` subcommand through a narrow Typer group parse shim. The new RFC 0058 stage-3 `kayakgen stability` sub-app (D-9) exposes `ingest-rig-run`, `promote-fixture`, `accept-fit`, and `residual-plot`; the legacy invocation form continues to evaluate initial / equilibrium stability for a hull, undocumented in `--help`. | Workflow 0056's traceability review (NB-1) accepted the shim during the daemon run; explicitly recording it here so the choice survives future CLI cleanup. Without the shim, registering the new sub-app would have broken the existing `kayakgen stability hull.json` invocation form. | `kayakgen/cli/stability_cli.py` carries the routing shim; `kayakgen/cli/main.py:505` is the legacy command definition (`@stability_app.command("legacy", hidden=True)`). Default `kayakgen stability hull.json --out ...` continues to write the same stability JSON; the four new subcommands shipped per RFC 0058 stage 3. | If a successor CLI cleanup RFC removes the legacy invocation form (with a deprecation cycle), or if the legacy command's behavior diverges from the new sub-app's schema-only commands. |
| D041 | accepted | Adopt the RFC 0059 `code_doc_audit` workflow shape as the standing periodic-audit cadence for kayak-gen: three independent lanes (pipeline-integrity / docs-decision-drift / operator-adoption) -> synthesis -> remediation plan, dispatched via `docs/workflows/0029-code-doc-audit/`. Default cadence is `full_repo` quarterly plus a `release_candidate` preset before any `CHANGELOG.md` entry that touches a public CLI or schema. | The 2026-05-22 dogfood run produced 13 evidence-backed findings and closed five high-severity items the discipline checklist would otherwise have missed; without a recurring shape the lane discipline (no cross-pollination before synthesis) would not persist across operators. | RFC 0059 lands at `Status: landed`. Workflow 0029 is the canonical entrypoint; presets `full_repo` / `rfc_cluster` / `release_candidate` / `subsystem` / `adoption_path` cover the foreseen scopes. Future audit runs write under `docs/audits/<YYYY-MM-DD>-code-doc-audit/` and reference this row by cadence rule, not as a separate decision. | If the lane diversity requirement changes (e.g. provider-diversity becomes mandatory), if a successor RFC widens the three-lane shape, or if the cadence proves wrong in practice (too frequent / too rare for the project's landing pace). |
| D042 | accepted | Add `EMPTY_STABILITY_FIT_REGISTRY: tuple["StabilityFitRecord", ...] = ()` on `kayakgen/eval/stability/accepted_fit.py` and require the three RFC 0058 stage-2/3 call sites (`kayakgen/eval/stability/evaluator.py`, `kayakgen/ui/web/generate_frontier_view.py`, `kayakgen/ui/web/generate_spec_form.py`) to consume the constant rather than the bare `()` literal. The constant's docstring names D039 and the stage-4 graduation gate. | Audit finding AUD-P-002 (workflow 0030 R7) showed that the three call sites' identical hard-coded empties created an invisible synchronization contract — a future maintainer wiring a real registry could easily miss one site and land an inconsistent promotion state. Naming the empty makes the contract auditable and gives the stage-4 graduation a single replacement point. | Behavior is byte-stable; the constant is the single source of truth for "no fits accepted yet" through stage 3. Stage 4 graduation requires replacing one constant import, not three literals. | If RFC 0058 stage 4 lands and the registry becomes call-site-specific (e.g. UI surfaces want a filtered subset), or if the registry implementation becomes lazy and the constant pattern stops being adequate. |
| D043 | accepted | Adopt the RFC 0060 `HullParameterMetadata` presentation-layer registry as the pattern for UI-facing labels and tooltips on hull-parameter form fields: a frozen Pydantic value object + a keyed-by-JSON-name registry in `kayakgen/ui/parameter_metadata.py`, consumed by `kayakgen/ui/web/generate_spec_form.py` for the variable picklist, base-hull rail, and (via the existing `OBJECTIVE_METADATA.label / .unit`) the objectives picklist. The form's submitted JSON payload stays byte-stable; the registry is purely additive on the wire. | Audit finding AUD-O-003 (workflow 0033) confirmed the web Generate panel exposed raw JSON keys to non-developer kayak designers while the desktop GUI's `SLIDERS` table already had friendly labels. A small presentation-layer registry is the simplest pattern that closes the UX gap without coupling the form schema to the search / domain layer. | `kayakgen/ui/parameter_metadata.py` ships the value object + 11-row registry + `label_with_unit(parameter)` and `description(parameter)` helpers. `tests/test_hull_parameter_metadata.py` and `tests/test_vocabulary_coverage.py` pin the contract. Future UI fields that need friendly labels should land their entries in this registry rather than re-defining the pattern. RFC 0061 (workflow 0034) closed the named desktop-migration follow-up: desktop `SLIDERS` now consumes the same registry, the GUI/Hull key drift is gone, and `VIEW_PARAMETER_METADATA` owns the view-only `target_speed_kt`. | If objective descriptions land on `ObjectiveMetadata` directly rather than the parallel presentation-layer registry, or if i18n is later accepted. |
| D044 | accepted | Apply the D043 "presentation-layer registry per surface family" pattern to the Hydro tab's hydrostatics rows: land `HydrostaticsRowMetadata` (frozen Pydantic value object with `parameter`, `label`, `unit`, `description`) + a 7-row `HYDROSTATICS_ROW_METADATA` registry under `kayakgen/ui/hydrostatics_metadata.py`, and have `kayakgen/services/evaluation.py::analysis_view_model::hydro_rows` source labels and units from the registry. The `(label, value, unit)` tuple shape, value formatting (`:.1f` / `:.3f` / `:.2f`), and `hydro_rows_from_state` wire payload are all byte-stable. | Audit finding AUD-O-005 (2026-05-25 release_candidate audit, workflow 0038) named the hardcoded inline tuples in `analysis_view_model` as a small UX gap: any label change had to touch two places and there was no single documentation surface listing what each hydrostatics row means. Mirroring the D043 / RFC 0060 / RFC 0061 pattern is the cheapest fix. | RFC 0062 lands as `landed` and indexes in `docs/rfcs/README.md`. `tests/test_hydrostatics_row_metadata.py` (12 tests) covers registry coverage, count pin, well-formedness, wiring through `analysis_view_model`, and a byte-stable wire-payload regression assertion against the post-`b82b544` baseline. Future sibling registries (mesh-diagnostic metadata, evaluator-status metadata, resistance-row metadata) are flagged as candidates in RFC 0062 but not in scope. | If hydrostatics rows need internationalization, if the registry needs per-evaluator variants (calibrated vs uncalibrated rows), or if a successor RFC consolidates hydro / mesh / parameter registries into a unified surface-metadata model. |
| D035 | accepted | Land RFC 0054 calibration-campaign tooling as four `kayakgen calibration` subcommands (ingest-tank-test, ingest-inclining-test, accept-fit, residual-plot) with five new Pydantic schemas (`RightsChecklist`, `GeometryReference`, `TankTestRun`/`TankTestCampaign`, `IncliningTestRun`/`IncliningTestCampaign`, `AcceptedFitRecord`). The `ResistanceSourceReviewPacket` validator now resolves `accepted_fit_ref` on disk for `.json`-pathed refs and refuses below-threshold fits with structured tokens. | The 2026-05-17 wave implemented RFC 0054 with these defaults pre-resolved: `RightsChecklist` lives in `kayakgen/eval/calibration/rights.py`; hull binding by `design_hash` (falls back to `Hull.hash()` until RFC 0049 lands the explicit method, then the binding auto-upgrades); default `fit_metric=RMSE` with a 5%-of-baseline ceiling; `multi_chine_count` defaults to 2. | New module `kayakgen/eval/calibration/campaigns.py` carries the campaign aggregates; `kayakgen/services/calibration_artifacts.py` carries the vendored SVG residual-plot writer (no matplotlib dependency in the writer path). Structured tokens: `accepted_fit_unresolved`, `accepted_fit_unparseable`, `fit_above_rmse_threshold`, `fit_above_mape_threshold`, `fit_below_r2_threshold`. Edinburgh stays at `validation_fixture`; the test uses a synthetic source for the calibration_fixture promotion. +19 tests. | If a fourth ingest format is needed, if the per-call threshold floor changes, or if a kayak-envelope measured source unblocks D006 and the validator needs tighter enforcement. |
| D034 | accepted | Land RFC 0053 turning + edged-waterline metrics as an opt-in `--turning [--turning-heel-deg N]` flag on `kayakgen evaluate` plus an `evaluators.turning_metrics` flag on sweep. The four numeric metrics (`edged_waterline_length_m`, `upright_waterline_length_m`, `lateral_plane_shift_m`, `rocker_weighted_maneuverability_signal`) are registered with `role="display_only"` so they are refused as Pareto/search objectives. | The 2026-05-17 wave implemented RFC 0053 with default heel 8°, raw units (m, m²), combined rocker-weighted signal in v1 (not bow/stern split). The metrics are a geometric proxy, not a turning prediction. | `kayakgen/eval/turning.py` carries `TurningMetrics` + `evaluate_turning_metrics`; the four registry entries are in `kayakgen/search/objectives.py`. Default evaluate JSON is byte-stable without the flag. +14 tests. | If a successor RFC promotes the metrics from display-only to objective-admissible (would require measured calibration of the proxy against on-water turning data), or if the bow/stern rocker split becomes useful. |
| D033 | accepted | Land RFC 0052 sensitivity + uncertainty as an opt-in `kayakgen sensitivity` CLI plus additive `convergence: list[ConvergenceFlag]` on `EvaluationResult` and a `pairwise_notes` block on `ComparisonReport`. Auto-step is `1e-4 * baseline_value` clamped to `[1e-9, 1e-2]`; tolerance map lives in the metric registry as `ObjectiveMetadata.within_evaluator_noise_threshold`. | The 2026-05-17 wave implemented RFC 0052 with the registry as the single tolerance source of truth, default per-metric thresholds (`GM0_m=0.001 m`, `displacement_error_kg=0.5 kg`, `mesh_problem_count=1.0`), and the sign-check substitution `length_m → displaced_mass_kg` for the regression test (the lofted hydrostatic model is length-invariant for `GM0_m`). | `kayakgen/services/sensitivity.py` ships `SensitivityRequest` / `SensitivityResult` / `compute_sensitivity`; `ConvergenceFlag` and `PairwiseNote` are additive value objects. +11 tests. | If a successor RFC adds Monte-Carlo uncertainty propagation, covariance matrices, or a different step-selection heuristic. |
| D032 | accepted | Land RFC 0051 builder-oriented exports as a new `kayakgen build-export` CLI under a new `[builder]` extras group (ezdxf). Seven artifacts plus `manifest.json` per invocation. Default 1:1 mm scale; deterministic across two invocations modulo CAD-library save timestamps. | The 2026-05-17 wave implemented RFC 0051 with these defaults: `ezdxf >= 1.1` as the optional DXF library; SVG written by hand (no library); separate subcommand instead of a flag on `kayakgen generate`. Determinism on the DXF path compares parsed entities, not raw bytes. | `kayakgen/services/build_export.py` ships the seven writers + `write_build_export` orchestrator. Each artifact carries a header comment with `Hull.hash()` + the kayakgen version pin. +11 tests (10 + 1 module-level `pytest.importorskip("ezdxf")`). | If a different DXF library wins stronger evidence, if the operator wants a multi-sheet plot layout, or if a future RFC widens the file types (e.g. STEP, IGES). |
| D031 | accepted | Land RFC 0050 target-draft / target-trim workflows as two new CLI subcommands (`kayakgen target-draft`, `kayakgen target-trim`) wrapping existing equilibrium solvers. `--report-only` on target-draft emits a `TargetDraftMismatchReport`. Loads exceeding 2× max displaced mass are refused with a structured `load_unphysical` error. | The 2026-05-17 wave implemented RFC 0050 with no `--target-load` flag on `kayakgen evaluate` (keeps that command's surface clean), no `--initial-draft` hint in v1, and load-mismatch reporting as a `--report-only` flag rather than a third subcommand. | `kayakgen/services/evaluation.py` ships `solve_target_draft`, `solve_target_trim`, `target_draft_load_mismatch`; `kayakgen/cli/target_workflows.py` is the thin Typer adapter. +9 tests. Default `kayakgen evaluate`/`stability` output unchanged. | If a successor RFC needs multi-load batch optimization (covered by RFC 0044/0047 active search) or a `--target-displacement` shortcut without a load-case file. |
| D030 | accepted | Land RFC 0049 ArtifactStore + identity normalization as `Hull.record_hash()` + `Hull.design_hash()` (existing `Hull.hash()` aliases `record_hash`, byte-stable), `kayakgen.services.identity` (`record_hash`, `design_hash_for_hull`, `run_hash`), and `kayakgen.services.artifact_store` (`FilesystemArtifactStore` + `SqliteIndex` at `~/.local/share/kayakgen/index.sqlite`, overridable via `KAYAKGEN_INDEX_DB`). Sweep, search, and CFD writers route through the store; canonical paths and bytes stay byte-stable. New `kayakgen runs {list,query,reindex}` Typer sub-app. | The 2026-05-17 wave implemented RFC 0049 with these defaults: machine-wide single SQLite DB (one cross-run view, simpler than per-run DBs); hard-links with copy-on-cross-device fallback + warning; missing `_store/` re-derives from canonical paths on read with a warning; store interface lives in `kayakgen.services`; `design_hash` includes `geometry_kind` so a v1 `lofted` hull and a v2 `distribution_v2` hull never collide. | Identity module + store module + index schema (per the RFC). Legacy run directories without a `_store/` still load and produce honest warnings. +17 tests. Backwards compatibility: every existing `Hull.hash()` output is unchanged. | If a remote/cloud backend is later accepted, if the index location moves, or if a future RFC adds cross-run correlation primitives (event streams, materialized views). |
| D029 | accepted | Land RFC 0048 Geometry V2 distribution model as an additive `geometry_kind: Literal["lofted", "distribution_v2"]` on `Hull` (default `"lofted"`) plus a `DistributionV2Spec` value object carrying five `LongitudinalDistribution` records, six cross-section families (`round`, `shallow_arch`, `shallow_v`, `deep_v`, `hard_chine`, `multi_chine` with 2-4 chines), explicit rocker / LCB / deadrise / chine-radius / bow-flare controls. Hydrostatic cross-check (1% volume / 1% Aw / 1% LCB / 0.5% GM0) is advisory-only. Migration writes a sibling `<name>.v2.json`. Non-default `bow_rake` / `stern_rake` refused on v2. | The 2026-05-17 wave implemented RFC 0048 with these defaults: 1.0% hydrostatic tolerance (looser than the 0.5% the RFC's body suggested, to account for existing 0.1% loft drift); sibling-file migration (avoids overwriting the source); `multi_chine_count: int = 2` (parametrized 2-4); deadrise + chine radius admit `float | LongitudinalDistribution`; rocker is a distribution; legacy rake fields kept for reporting but refused if non-default on v2. | `kayakgen/model/distribution_v2.py` + new `DistributionV2Geometry` in `kayakgen/model/geometry.py`; canonical closed-body source of truth (open inspection sections are derived from it); hydrostatic cross-check lives in `kayakgen/eval/hydrostatics.py` and emits `Hydrostatics.notes` + an optional `v2_cross_check: V2HydrostaticCrossCheck` block; new `kayakgen migrate-geometry` CLI. Default lofted hull STL + hydrostatics stay byte-stable. +77 tests. | If a measured-validation source unblocks the geometry choice envelope, if a 3D-printed cross-section family is needed, or if a future RFC widens the chine count beyond 4 / adds a `geometry_kind="distribution_v3"` superseding this one. |
| D028 | accepted | Land RFC 0047 v2 active-search EHVI as the successor to v1 NSGA-II. EHVI is the additive opt-in expressed via `algorithm.kind = "ehvi"` on `SearchAlgorithmSpec`; 1-objective EHVI reduces to expected-improvement Bayesian optimization, so the single code path covers both single- and multi-objective use. MOEA/D rejected for v2 (overlaps NSGA-II without solving cost-per-eval). | RFC 0047's recommendation; the cowboy 2026-05-16 "do 4" session implemented it inside the repo with no external optimization-library dependency. EHVI is the canonical multi-objective Bayesian-optimization acquisition function, and the expensive evaluator path (real OpenFOAM-v2512 interFoam under D012 / D026) benefits most from expected-cost optimization. | `kayakgen/search/active/gp.py` ships a vendored Cholesky-factorized GP (Matern 5/2 + RBF kernels) with a vendored Nelder-Mead marginal-likelihood optimizer; numpy only. `kayakgen/search/active/ehvi.py` ships EHVI for 1-3 objectives with `EhviDimensionError` for 4+. Runner dispatches on `EhviAlgorithmConfig`; RFC 0043 + RFC 0044 gates apply unchanged. Surrogate predictions never enter `summary` or `run.json` objectives. +19 tests; full suite 604 + 2 skipped. | If a non-axis-aligned EHVI decomposition is needed for 4+ objectives, if a different surrogate (e.g. deep kernels, random forests) wins stronger evidence, or if a future RFC accepts an external GP library. |
| D027 | accepted | Land RFC 0046 three-mechanism opt-in for the OpenFOAM-v2512 `succeeded` path with precedence profile_flag > persistent_setting > env_knob, retaining `KAYAKGEN_OPENFOAM_LOCAL_RUN=1` as the backwards-compatible bottom rung. Default behavior (no opt-in) continues to return `solver_success_blocked`. | RFC 0046's recommendation; the cowboy 2026-05-16 "do 4" session implemented it. The env-only gate from D022 was honest but operationally awkward (re-typing per shell, per invocation). A per-job profile flag is the most explicit opt-in (travels with the job artifact); a persistent setting is the most ergonomic (one-time per workstation); the env knob stays as the test-time / scripted fallback. | `kayakgen/eval/cfd/jobs.py` extended with `RealSolverExecutionOptIn` literal, `SolverExecutionAudit` model, `resolve_real_solver_execution_opt_in` resolver, and audit-trail fields on `CfdRunRecord`. New `kayakgen/eval/cfd/config.py` ships `KayakgenCfdConfig` Pydantic model + loader at `~/.config/kayakgen/cfd.json` with structured rejection codes (`cfd_config_malformed_json`, `cfd_config_schema_mismatch`). `kayakgen/cli/main.py` adds `--allow-real-solver-execution` to `cfd prepare`. `claim_state` stays `raw_unvalidated`; `accepted_uses` stays `[]`; case_template_version stays locked. +11 tests; full suite green. | If a fourth opt-in mechanism is needed (e.g. project-scoped config), if the precedence ordering proves wrong in practice, or if the env knobs are deprecated by a successor RFC. |
| D026 | accepted | Land RFC 0045 ordinary-package solver-readiness promotion through explicit hash-bound evidence binding. `kayakgen mesh-evidence` runs the meshing stage and emits a serialized `SnappyHexMeshEvidence`; `kayakgen mesh-package --bind-evidence` attaches that evidence to a watertight mesh package's manifest, which then reports `cfd_ready` through the existing readiness gate iff all three hash checks pass. Default `kayakgen mesh-package` is byte-equal to before when `--bind-evidence` is absent. | RFC 0045's recommendation; the cowboy 2026-05-16 "do 4" session implemented it. Auto-attaching evidence at package time would have re-opened the D003 / D011 / D012 / D022 conservatism by making cfd_ready feel cheap; making the bind step explicit and three-way hash-checked preserves the audit trail. | New `bind_evidence_to_mesh_package(evidence, *, closed_body_hash, polymesh_dir) -> VolumeMeshDiagnostic` in `kayakgen/eval/snappy_hex_mesh.py`. `MeshEvidenceBindError` carries a `code` from `MeshEvidenceBindCode` (`closed_body_hash_mismatch`, `snappy_evidence_body_mismatch`, `polymesh_artifact_drift`, `evidence_not_recorded`, `evidence_translation_failed`). +7 tests. No new readiness label, no relaxation of cfd_ready semantics, no auto-promotion path. | If the three-hash gate proves too strict (e.g. genuine polyMesh re-rendering use cases), if a fourth rejection code is needed for partial evidence, or if a successor RFC accepts an auto-bind path during `kayakgen mesh-package`. |
| D025 | accepted | Promote the Edinburgh DataShare Pacific-canoe packet from `validation_candidate` to `validation_fixture` with a documented uncertainty caveat, relax `ResistanceSourceReviewPacket` validators in two narrow ways: (a) `validation_fixture` may carry `non_promotion_reasons` that describe blockers against the next-level promotion (`calibration_fixture`); (b) `validation_fixture` may have `uncertainty.status == "incomplete"` IF AND ONLY IF `uncertainty_notes` is non-empty AND the `warnings` list contains `uncertainty_documented_caveat`. | D013 intended Edinburgh to be promotable to `validation_fixture` once acquisition completed, but the validator's two pre-existing rules conflated "blockers against THIS verdict" (correctly forbidden on fixtures) with "blockers against the NEXT promotion" (legitimately retained as calibration blockers). The cowboy 2026-05-16 acquisition revealed that the workbook publishes averaged rows without per-row repeatability — uncertainty data that genuinely cannot be marked `accepted`, but whose absence is a known documented limitation rather than missing evidence. | Two new module-level tokens (`VALIDATION_FIXTURE_ADMITS_CALIBRATION_BLOCKERS`, `VALIDATION_FIXTURE_ADMITS_DOCUMENTED_UNCERTAINTY_CAVEAT`) name the relaxed rules. Edinburgh now has `review_verdict="validation_fixture"`, `source_use="validation_fixture"`, `fixture_id="edinburgh-pacific-canoe-hydrodynamics-2022-11-14-v15-imv"`, `fixture_version="1"`, `accepted_uses=["validation_only"]`, full `validity_envelope`, full `ResistanceSourceRecord` fixture metadata, and `fixture_review_status="accepted"`. `non_promotion_reasons=["outside_sea_kayak_calibration_envelope"]` is preserved as the calibration blocker. `calibration_promotion_blockers()` still returns the accepted-fit requirement plus `incomplete_source_review_evidence` for the uncertainty caveat. `calibration_fixture` itself cannot carry non-promotion reasons (no further promotion exists). +2 tests; full suite 566 passed + 2 skipped. | If a calibration-grade kayak-envelope source replaces Edinburgh's validation role, if the workbook ever gains per-row uncertainty rows, or if a successor RFC re-tightens the validator to require complete uncertainty for all fixtures. |
| D024 | accepted | Land RFC 0044 v1 active hull-design search as a vendored, pure-Python NSGA-II (no external optimization-library dependency), additive on top of `kayakgen sweep` via a new opt-in `kayakgen search` CLI subcommand. The v1 surface is multi-objective only; single-objective use is expressed as one-objective NSGA-II. | RFC 0044 (proposed earlier in this session) scoped the surface and the admissibility rules; the operator authorized "continue working until there's no more work or explicitly asked to stop" and the implementation was ready to land without external resources. A vendored implementation avoids supply-chain risk and lets the seeded-determinism contract hold without a third-party algorithm release cadence. | `kayakgen/search/active/` ships `spec.py`, `nsga2.py`, `constraints.py`, `runner.py`, and `__init__.py`. `ensure_objectives_claim_admissible_for_search` in `kayakgen/search/pareto.py` (token `RFC_0044_SEARCH_OBJECTIVE_CLAIM_ADMISSIBILITY`) refuses `raw_unvalidated` and `uncalibrated_comparative` objectives unless `objectives_explicit_exploratory: true` is set; the RFC 0043 high-angle-GZ refusal always wins on top. `kayakgen sweep` and `kayakgen compare` defaults are unchanged. +32 tests; full suite 564 passed + 2 skipped (env-gated smoke). | If a successor algorithm RFC lands (Bayesian optimization with GP surrogates, EHVI hypervolume improvement, MOEA/D), if the seeded-determinism contract becomes load-bearing for a calibrated workflow, or if an external optimization-library dependency is later accepted by an RFC. |
| D023 | accepted | Defer the RFC 0030/0032/D008/D015 public hosted demo indefinitely. No operator owner, budget cap, deployment SHA, hosted smoke, persistence policy, cleanup receipt, or public no-claims wording is being pursued by this project at this time. | The 2026-05-16 cowboy operator review confirmed there is no current need for a public URL or hosted browser demo. The `kayakgen serve` and repo Docker paths remain the only supported browser surfaces, used locally only. | RFC 0030 and RFC 0032 stay at their current landed-local-only state. The `docs/WEB_VERIFICATION.md` runbook continues to describe the narrow local/Docker path. Future browser-hosting work requires an explicit successor decision and is no longer on the active backlog. | If an operator owner and budget cap are recorded and a successor RFC reopens hosted demo scope, or if a static/Pyodide path is explicitly accepted by a future RFC. |
| D022 | accepted | Close D012's `openfoam-v2512-interfoam-local` `succeeded` gate by wiring a real OpenFOAM-v2512 case renderer + executor + evidence binder behind two opt-in env knobs (`KAYAKGEN_OPENFOAM_LOCAL_RUN=1` flips the adapter's path; `KAYAKGEN_OPENFOAM_SMOKE=1` enables the integration test). The bashrc-sourced subprocess helper accepts an override via `KAYAKGEN_OPENFOAM_BASHRC`. | The cowboy 2026-05-15 session confirmed OpenFOAM-v2512 is installed at `/usr/lib/openfoam/openfoam2512/`. The case-template lock, provenance probe, and v2512-correct force.dat parser were already landed, but `succeeded` stayed blocked because no real mesh evidence existed. A real smoke run (default Hull → closed body → blockMesh + surfaceFeatureExtract + snappyHexMesh + checkMesh + setFields + interFoam) takes ~10.7s and produces a real `force.dat` with 10 fields per row, contradicting the earlier 19-field assumption — the parser was corrected and fixtures regenerated. | `kayakgen/eval/cfd/openfoam_v2512_interfoam/` hosts the case templates, `case_render.py`, `runner.py`, and `evidence.py`. `OpenFoamLocalAdapter.run` now branches into `_attempt_real_succeeded_path(...)` only when both env knobs are set and `is_openfoam_available()` is true; the returned `CfdOpenFoamRawResult` preserves `claim_state="raw_unvalidated"`, empty `accepted_uses`, and the locked `case_template_version`. Default behavior is byte-equal to the prior `solver_success_blocked` path when the env knobs are absent. | If a different solver/case-template is selected, if validated CFD output is accepted by a future RFC, or if hosted execution becomes operationally required. |
| D021 | accepted | Keep desktop GUI support for high-angle GZ intentionally absent in this cowboy pass — stage 4 in workflow 0052's staged surfacing is "desktop minimal/supporting", and the desktop has no comparison/sweep panel today, so the smallest correct delivery is no GUI change. | Stage 3 landed CLI/sweep/comparison/web surfaces with the shared `high_angle_gz_display` read model. The desktop's status segments are summary chips, not a comparison surface. Adding a high-angle indicator would either duplicate state already shown by the CLI/web paths or change defaults that the staged surfacing forbids. | The desktop GUI is unchanged. Users who want high-angle GZ output run `kayakgen stability --high-angle-gz` or open the sweep/comparison surfaces in the web workspace. Stage 4 is recorded as "intentionally minimal" rather than "deferred". | If a recorded user need emerges for desktop high-angle GZ, or if the desktop gains a comparison/sweep panel that could host the read model under shared claim boundaries. |
