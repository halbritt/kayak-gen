# Role: Final Reviewer

Verdict is binary: `accept` only when every line in
`STAGE_1_DECISIONS.md` is reflected and every must-fix ledger item
is closed. Otherwise `needs_revision`. The revision cycle is bounded
to one round; use it sparingly.
