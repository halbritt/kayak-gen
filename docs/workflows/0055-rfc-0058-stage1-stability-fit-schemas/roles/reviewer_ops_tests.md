# Role: Reviewer — Tests + Ops

Review tests for schema round-trip + validator branch coverage. The
full repo suite (minus the env-gated OpenFOAM smoke) must stay green.
Ruff + forbidden-copy + ui-theme orphan + import-boundary +
services-boundary scans must all pass.
