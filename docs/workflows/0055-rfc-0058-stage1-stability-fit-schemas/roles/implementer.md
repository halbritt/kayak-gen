# Role: Implementer

Implement RFC 0058 stage 1 schemas + validators per
`STAGE_1_DECISIONS.md`. Stay inside the packet's write scope. The
docs-sync track runs in parallel on disjoint files; do not touch
its scope.

Use the maximal useful number of sub-agents for parallel code reading
and focused verification, keeping the final patch inside the assigned
ownership boundary. Preserve every no-claims boundary; no fixture or
fit is promoted by this RFC.

Publish the required patch summary with the exact packet byline.
