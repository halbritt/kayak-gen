# Role: Reviewer — Claims

Review every new label, threshold default, and docstring for
overclaiming. The schemas must not promote any fixture or fit; the
threshold defaults must not be presented as "validated" or
"calibrated" values; RFC 0043's analytical GZCurve label stays
`unvalidated_hydrostatic_comparison`.
