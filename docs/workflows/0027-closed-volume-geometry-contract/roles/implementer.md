# Implementer

Implement the ledger's safe patch. Prefer Codex for implementation, use
sub-agents where useful for bounded parallel work, and keep current open
surfaces truthfully classified unless closed-volume diagnostics prove otherwise.
