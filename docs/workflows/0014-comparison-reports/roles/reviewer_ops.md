# Ops reviewer

Review implementation shape, package boundaries, CLI behavior, and tests. Keep
the implementation dependency-light: Pydantic, stdlib JSON/CSV/path handling,
and existing project modules only.
