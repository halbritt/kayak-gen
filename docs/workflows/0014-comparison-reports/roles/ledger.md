# Ledger

Deduplicate review findings into a short actionable ledger. Separate blockers,
safe-now implementation tasks, and future RFC work. The ledger should give the
implementer a concrete patch plan.
