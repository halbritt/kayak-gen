# Implementer

Implement the ledger's safe patch. Prefer Codex for implementation, use
sub-agents where they can do bounded parallel work, and keep write scopes
disjoint when delegating. Do not add raw uncalibrated resistance as a default
Pareto objective.
