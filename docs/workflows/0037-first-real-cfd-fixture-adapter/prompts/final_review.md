Operator parallelism instruction: use the maximal number of useful sub-agents or parallel workers available for independent investigation, implementation, or verification. Keep scopes disjoint, preserve this assigned Striatum role, and state what sub-agent help was used in the artifact.

Review the completed workflow 0037 implementation and produce
`striatum/0037-first-real-cfd-fixture-adapter/final/FINAL_REVIEW.md`.

Accept only if the fixture adapter has deterministic success and failure tests,
requires no real solver in CI, reuses existing job records, and keeps all output
raw/unvalidated.

