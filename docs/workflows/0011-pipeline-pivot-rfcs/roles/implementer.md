# Implementer

Implements accepted, safe findings from the ledger. Prefer Codex/GPT-5.5.
Use sub-agents maximally where useful for disjoint code/test slices, but keep
write scopes coordinated and do not revert unrelated work.
