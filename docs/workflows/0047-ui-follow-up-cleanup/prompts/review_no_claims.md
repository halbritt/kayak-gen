Read `docs/workflows/0047-ui-follow-up-cleanup/SOURCES.md`, drafted RFC 0035,
and the current UI wording.

Review no-claims boundaries only. Do not modify repo files outside the
required review artifact.

Check that the cleanup cannot imply:

- calibrated drag, final prediction, design fitness, or validated resistance;
- real CFD solver success, hosted/cloud CFD, or unavailable solver hiding;
- real high-angle `GZ` or secondary-stability availability;
- watertight solid, production volume mesh, or generated `cfd_ready` promotion;
- web-side artifact creation beyond already accepted safe UI entries.

Write `striatum/0047-ui-follow-up-cleanup/no_claims/REVIEW_NO_CLAIMS.md` as a
`finding` artifact with verdict intent:
`accept`, `accept_with_findings`, `needs_revision`, or `reject`.
