# Implementer Role

Implement only ledger-approved UI cleanup work. Use the maximal number of
useful sub-agents with disjoint write scopes. Prefer parallel agents for
independent UI, accessibility, tests, docs, and cleanup tasks, with one agent
responsible for final integration.
