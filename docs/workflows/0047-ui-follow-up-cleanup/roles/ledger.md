# Ledger Role

Deduplicate review findings into a small implementation ledger. Separate
safe-now cleanup from deferred desktop parity, backend capability, real solver,
calibration, and stability work.
