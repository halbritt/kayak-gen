Implement high-angle GZ only against the accepted closed-volume model. Prefer
Codex for code work. Use the maximal number of useful sub-agents with disjoint
write scopes. Prefer parallel agents for independent geometry, solver,
CLI/frontend, tests, and docs tasks, but keep one agent responsible for final
integration. Do not emit secondary stability values for open-surface packages.
