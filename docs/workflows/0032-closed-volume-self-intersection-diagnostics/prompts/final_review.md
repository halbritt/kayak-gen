Review the ledger and patch summary. Produce
`striatum/0032-closed-volume-self-intersection-diagnostics/final/FINAL_REVIEW.md`.

Verdict must be `accept` or `needs_revision`. Verify self-intersection
diagnostics are serialized, tested, readiness-gating is conservative, and no
generated body or CFD readiness claim was introduced.

