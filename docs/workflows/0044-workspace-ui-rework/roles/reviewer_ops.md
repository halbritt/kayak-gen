# Ops/test reviewer

Review CLI, REST, web, desktop, and accessibility surfaces, plus the
test coverage for the rework. Ensure every existing REST route keeps
its JSON shape, the share URL round-trip is preserved, and the
forbidden-claim regression tests cover every no-go string from RFC
0033 §8.
