# Role: Remediator

Fix every must-fix finding in the ledger. Keep fixes scoped; do not
introduce new design scope. Preserve all 12 stage-4 decisions and the
existing no-claims boundaries.

Use the maximal useful number of Codex sub-agents for disjoint remediation
tasks. Run the full repo suite minus the env-gated OpenFOAM smoke before
publishing the patch summary.
