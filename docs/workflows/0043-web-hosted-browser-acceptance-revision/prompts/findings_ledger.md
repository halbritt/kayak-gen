Operator parallelism instruction: use the maximal number of useful sub-agents or parallel workers available for independent investigation, synthesis, or verification. Keep scopes disjoint, preserve this assigned Striatum role, and state what sub-agent help was used in the artifact.

Read the review anchor artifact and all three 0043 review artifacts. If 0041
review artifacts are available, carry forward any still-actionable findings
instead of rediscovering them.

Produce
`striatum/0043-web-hosted-browser-acceptance-revision/ledger/FINDINGS.md`.
Do not add a plain byline whose first characters are `author:`.

Include a gate result, finding counts, deduplicated findings, implementation
guidance, verification expectations, and a deferred/out-of-scope list. Separate
safe-now local browser acceptance and hosted-demo documentation from public
hosted operation, custom JavaScript frontend work, full mobile editing,
comparison-dashboard parity, real solver execution, and calibrated or validated
CFD claims.
