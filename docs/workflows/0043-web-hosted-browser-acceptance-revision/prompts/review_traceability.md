Operator parallelism instruction: use the maximal number of useful sub-agents or parallel workers available for independent investigation or verification. Keep scopes disjoint, preserve this assigned Striatum role, and state what sub-agent help was used in the artifact.

Read `docs/workflows/0043-web-hosted-browser-acceptance-revision/SOURCES.md`
and the review anchor artifact.

Produce
`striatum/0043-web-hosted-browser-acceptance-revision/traceability/REVIEW_TRACEABILITY.md`.
Do not add a plain byline whose first characters are `author:`.

Focus on RFC 0032 traceability to RFC 0030, RFC 0008, RFC 0018,
`docs/WEB_VERIFICATION.md`, current user-guide status, expected tests, and
explicit deferrals. Verify that the scope does not claim hosted operation,
full dashboard parity, real CFD execution, calibrated resistance, or validated
solver output.

If the scope or artifacts are not traceable enough to implement safely, record
`needs_revision` with concrete required actions for the revision cycle.
