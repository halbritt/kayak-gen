# Implementer

Implement the ledger's safe patch with Codex preferred. Use sub-agents where
useful for bounded parallel work, keep one integrator responsible for the final
patch, and keep outputs raw/unvalidated unless calibration work has landed.
