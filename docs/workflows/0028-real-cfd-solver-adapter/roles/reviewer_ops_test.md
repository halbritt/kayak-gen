# Ops/test reviewer

Review solver dependency detection, local execution isolation, reproducible job
directories, stdout/stderr and failure capture, optional installed-solver tests,
and baseline tests that require no external solver binary.
