Implement only the accepted safe slice. Prefer Codex for code work. Use the
maximal number of useful sub-agents with disjoint write scopes. Prefer parallel
agents for independent source, ingestion, fitting/report, tests, and docs tasks,
but keep one agent responsible for final integration. If no dataset is accepted,
do not fabricate fixtures.
