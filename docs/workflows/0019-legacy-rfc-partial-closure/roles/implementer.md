# Implementer

Implement the ledger's safe patch. Prefer Codex for implementation, use
the maximal number of useful sub-agents with disjoint write scopes, and keep
writes inside the workflow scope. Do not overclaim watertight/end-cap behavior.
