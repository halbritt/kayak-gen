Implement only the ledger-approved watertight volume-mesh handoff. Use the
maximal number of useful sub-agents with disjoint write scopes. Prefer parallel
agents for independent generated-body diagnostics, volume-mesh evidence,
manifest/dispatch, CLI, tests, and docs tasks, but keep one agent responsible
for final integration. Do not promote open surfaces or synthetic fixtures to
`cfd_ready`.
