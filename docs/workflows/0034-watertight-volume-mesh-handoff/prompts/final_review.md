Review the ledger and patch summary. Produce
`striatum/0034-watertight-volume-mesh-handoff/final/FINAL_REVIEW.md`.

Verdict must be `accept` or `needs_revision`. Verify that `cfd_ready` is
profile-scoped, evidence-derived, and unavailable for open surfaces, stale
diagnostics, forged manifests, and synthetic closed-volume fixtures.
