Read `docs/workflows/0015-mesh-package-profile/SOURCES.md`, especially RFC
0010, `kayakgen/eval/mesh_diagnostics.py`, and the human decisions in the 0011
ledger.

Produce `striatum/0015-mesh-package-profile/domain/REVIEW_DOMAIN.md` with:

- author line: `author: operator [self-declared: operator-domain-review]`
- verdict intent
- findings `D-001`, `D-002`, ...
- required action for each finding

Focus on:

- open wetted-surface solver profile vs future watertight solid profile;
- stern-positive coordinate metadata;
- part identity and waterline semantics;
- readiness levels and solver profile attachment;
- what manifest warnings must remain visible.
