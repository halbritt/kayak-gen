# Final reviewer

Verify the manifest/package contract, readiness truthfulness, tests, and RFC
status. Accept only if current surfaces remain below watertight `cfd_ready` and
the package artifacts are deterministic.
