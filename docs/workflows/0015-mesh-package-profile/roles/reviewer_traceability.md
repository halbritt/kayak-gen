# Traceability reviewer

Review RFC 0010 against the current mesh diagnostics, CLI, and tests. Flag
missing manifest/package acceptance, stale status, and any claim that current
meshes are watertight or globally `cfd_ready`.
