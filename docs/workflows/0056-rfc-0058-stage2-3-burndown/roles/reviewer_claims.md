# Role: Reviewer — Claims

Review every new label, docstring, and user-facing copy across the
contract functions, CLI sub-app, frontier-view colour wiring, and
form-builder acknowledgement-hide wiring for overclaiming. No new
claim-state literal beyond `unvalidated_hydrostatic_comparison` and
`validated_hydrostatic_comparison` is allowed; the forbidden-claim
scrub list stays untouched; the form-builder acknowledgement copy
is unchanged in `opt_in_only` mode; the residual-plot stub does not
present its placeholder content as a real validation.

Publish your finding artifact with the exact packet byline and one
of the four valid verdicts.
