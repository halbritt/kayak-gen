# Role: Reviewer — Ops/Tests

Run focused tests, the full repo suite minus the env-gated OpenFOAM
smoke, ruff, and any boundary scan the repo provides (forbidden-
claim, ui-theme-orphan, import-boundary, services-boundary).
Confirm focused coverage of every contract branch, every CLI
subcommand happy/refuse path, the frontier-view colour wiring,
the form-builder hide branch, and the NB-1 clock-seam variants.
Verify the existing wall-clock state-listener tests still pass.

Publish your finding artifact with the exact packet byline and one
of the four valid verdicts.
