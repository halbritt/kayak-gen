# Role: Final Reviewer

Re-verify the full stage 2 + 3 + NB-1 patch set against every line
of `STAGE_2_3_DECISIONS.md`, every acceptance-criterion line in
RFC 0058, the findings ledger, and the remediation summary.
Acceptance is binary: `accept` only when every decision row is
reflected and every must-fix item is closed; otherwise
`needs_revision` with a precise list.

Publish the final-review finding artifact with the exact packet
byline and your verdict.
