# Implementer

Implement the ledger's safe patch. Prefer Codex for implementation. Use the
maximal number of useful sub-agents with disjoint write scopes. Keep one agent
responsible for final integration. Do not fake solver success and do not hide
unavailable solver states.
