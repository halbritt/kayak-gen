# Coordinator

Own workflow state, operator reporting, and queue hygiene. This is the
watertight solid mesh backlog workflow; do not claim `cfd_ready` unless final
review accepts a named profile with passing closure/manifold evidence.
