# Ops reviewer

Review `kayakgen serve`, Docker packaging, optional web dependencies, REST
helpers, and tests. Keep deployment instructions reproducible and avoid adding
heavy tooling unless the environment already supports it.
