# Bug-hunt surface coverage

| Surface | Last searched | Findings ids | Lookback notes |
|---|---|---|---|
| `kayakgen/cli/` | 2026-05-29 (search #2) | BUG-007 (high), BUG-008 (medium), BUG-009 (low), BUG-010 (low), BUG-073 (medium), BUG-074 (medium), BUG-075 (low), BUG-076 (medium), BUG-077 (low) | CLI subcommand argument validation, error-path coverage, exit codes. Tick 4 found path traversal + host/port validation + bounds gaps. Tick 25 re-search applied the 10-pattern lens: `cfd prepare` accepts unvalidated `--speed-mps` / `--seawater-density-kg-m3` / `--kinematic-viscosity-m2-s` (positivity / finite gaps), 9 CLI read_text() sites missing UTF-8 encoding (4th UTF-8 instance), `kayakgen stability legacy --high-angle-gz` json.dumps without sort_keys (RFC 0049 contract), four float parameters (`--turning-heel-deg`, `--rmse-threshold`, `--step`, `--tolerance-percent`) accept NaN/inf (11th NaN-validator instance), calibration/target-workflow reads also missing UTF-8. Not settled (2 searches, 5 new findings). |
| `kayakgen/eval/calibration/` | 2026-05-29 (search #2) | BUG-011 (high), BUG-012 (high), BUG-013 (medium), BUG-049 (high), BUG-050 (high), BUG-051 (medium), BUG-052 (medium) | Fixture promotion validators, source-review packet acceptance, extractor edge cases, NaN handling. Tick 5 found empty-reasons gap, path-traversal in accepted_fit_ref, unchecked non_promotion_reasons tokens. Tick 19 went deeper: found hardcoded column indices in Edinburgh extractor (silent wrong-column extraction on workbook changes), silent NaN-to-0.0 conversion in extractor (data corruption), missing NaN/Infinity validators on TankTestRun/AcceptedFitRecord (mirrors BUG-023), and empty accepted_uses allowed for fixture verdicts. Four new high/medium findings; extractor and validator gaps are critical. Not settled (2 searches, 4 new actionable bugs). |
| `kayakgen/eval/cfd/` | 2026-05-29 (search #2) | BUG-003 (low), BUG-004 (medium), BUG-063 (medium), BUG-064 (medium), BUG-065 (high), BUG-066 (high) | Adapter dispatch, env-knob precedence, subprocess error handling, force.dat / case-template parsing. Tick 2 found dead code + env-var test gap. Tick 22 re-search applied the 8-pattern lens: CfdOpenFoamForceDatSample lacks NaN-validators (9th instance), case-render UTF-8 encoding gap (3rd instance), force.dat parser rejects forward-compatible field counts (OpenFOAM v2606+), CFD adapter evidence binding misses cross-field validation (complements BUG-060). Not settled (2 searches, 4 new findings). |
| `kayakgen/eval/closed_volume/` | 2026-05-29 (search #2) | BUG-019 (medium), BUG-020 (medium), BUG-021 (high), BUG-067 (medium), BUG-068 (medium), BUG-069 (medium), BUG-070 (high) | Geometry construction, self-intersection diagnostics. Tick 9 found float-equality + dead code + the BUG-014 cross-link masking. Tick 23 re-search applied the 9-pattern lens: `ClosedVolumeDiagnostics.signed_volume_m3` lacks NaN-validator (10th instance), `ClosedVolumeWaterlineMetadata.beam_wl_m` lacks gt=0 + finite-value constraint, exact float-equality on `normal_length == 0.0` in self-intersection (8th instance), and missing coherence-check between `readiness.level="closed_volume"` and signed_volume_m3 > 0 (10th cross-field instance). Not settled (2 searches, 4 new findings). |
| `kayakgen/eval/evidence/` | 2026-05-29 (search #2) | BUG-005 (high), BUG-006 (medium), BUG-058 (medium), BUG-059 (high), BUG-060 (medium), BUG-061 (medium), BUG-062 (medium) | RFC 0045 binding gate enforcement, artifact sanitization, dispatch_state ordering. Tick 3 found path traversal in polymesh artifact validation and dispatch_state gate ordering. Tick 22 re-search found: NaN-validator gap on CheckMeshSummary (mirrors BUG-023/048); hash case-sensitivity and format validation missing (BUG-059, BUG-061); empty artifact_checksums allowed in evidence_recorded state (gate bypass); patch name/marker special-char injection (BUG-062). Five new high/medium findings. Not settled (2 searches, 5 new actionable bugs).
| `kayakgen/eval/stability/` | 2026-05-29 (search #2) | BUG-001 (critical), BUG-002 (high), BUG-043 (medium), BUG-044 (high), BUG-045 (medium), BUG-046 (medium), BUG-047 (medium), BUG-048 (low) | `EMPTY_STABILITY_FIT_REGISTRY` consumption, accepted-fit lookup, GZ contracts. Tick 1 found RFC 0058 stage-4 discriminator gap + missing constrained-trim validator. Tick 18 re-search went deeper on `measured_fixture.py`: NaN-validator gap on FreeEquilibriumPoint/Trace/Row (mirrors BUG-023), high-severity missing monotonicity validator on FreeEquilibriumTrace.points (RFC 0056 docstring contract unenforced — verified), float-equality in LoadingConfiguration (6th instance), bare-float comparison in StabilityFitMetrics thresholds (5th instance), and accepted_at timestamp range gap. Re-search productive — 6 new findings on top of the original 2. Not settled (2 searches). |
| `kayakgen/io/` | 2026-05-29 (search #2) | BUG-022 (medium), BUG-023 (medium), BUG-024 (low), BUG-025 (low), BUG-078 (medium) | File-format readers/writers; round-trip fidelity; RFC 0049 artifact store. Tick 10 found encoding gaps (no explicit UTF-8), missing NaN/Infinity validators on ResistanceCurve/Hydrostatics, non-atomic write pattern, and print-not-logging. Tick 26 re-search: found BUG-078 where save_hull() and save_evaluation() use model_dump_json() without sort_keys, violating RFC 0049 canonical-order JSON encoding contract for artifact-store content-addressing. Strong positive baselines: load_hull uses model_validate_json; no path traversal; no archive handlers. Not settled (2 searches, 1 new critical-impact finding). |
| `kayakgen/model/` | 2026-05-29 (search #2) | BUG-014 (critical), BUG-015 (medium), BUG-016 (low), BUG-054 (medium), BUG-055 (medium), BUG-056 (medium), BUG-057 (medium) | Pydantic model validators (Hull, claim records, advisory). Tick 6 found the deck/draft cross-field gap + frozen=False on Hull. Tick 20 re-search went deeper on advisory.py / validity.py / distribution_v2.py / geometry.py: float-equality on bow_rake/stern_rake plumb detection in geometry.py:157-159 (7th instance), NaN-validator gaps on UniformDistribution.value + PolynomialDistribution.coefficients (5th instance), DistributionV2Spec (6th instance), and DesignAdvisory.l_over_bwl/cp/displaced_mass_kg (7th instance). Not settled (2 searches, 4 new pattern-instances). |
| `kayakgen/search/active/` | 2026-05-29 (search #1) | BUG-017 (low) | NSGA-II / EHVI math, seeded determinism, constraint refusal. Tick 7 confirmed strong positive baselines: seeded determinism end-to-end (no global RNG calls), NSGA-II tie-breaking deterministic, crowding-distance zero-div guarded, `ensure_objectives_claim_admissible_for_search` gate invoked at the right point, resume preserves the seed contract. Found only a low-severity `assert best_genome is not None` in the EHVI runner. Not settled (1 search), but future passes likely redundant unless RFC 0044/0047 changes land. |
| `kayakgen/search/` (non-active) | 2026-05-29 (search #1) | BUG-026 (high), BUG-027 (medium), BUG-028 (info, null) | Pareto, objective admissibility, sweep planner. Tick 11 found that `compare.py` calls `ensure_objectives_not_high_angle_gz` but never `ensure_objectives_claim_admissible_for_search` (RFC 0044 entry-point gate bypass — verified by grep) and `dominates()` in pareto.py uses bare `<`/`>` on floats (4th float-equality finding). Parent thread verified BUG-026 against source. Not settled (1 search). |
| `kayakgen/services/` | 2026-05-29 (search #2) | BUG-018 (info), BUG-040 (medium), BUG-041 (medium), BUG-042 (medium) | Tick 17 deeper audit of lighter-coverage modules (cfd_jobs.py, artifact_store.py, build_export.py, design.py, and 8 others). Found: (1) BUG-040: UTF-8 encoding gap in read_text()/write_text() across services (mirrors BUG-022); (2) BUG-041: TOCTOU race in FilesystemArtifactStore._put_bytes() concurrent write; (3) BUG-042: missing bounds validation on BuildExportSpec.n_stations. Not settled; encoding issue is systemic (io/ + services/). |
| `kayakgen/ui/desktop.py` | 2026-05-29 (search #1) | BUG-035 (medium), BUG-036 (low), BUG-037 (low, downgraded from medium/security), BUG-038 (low) | matplotlib slider wiring, PyQt event loop, deprecation shim. Tick 15 found class-preset narrowing missing a clamp guard (non-seeded sliders can retain out-of-range values), PyVista 3D-window cleanup gap, STL-export path not normalised (parent downgraded from subagent's medium/security — threat model is operator footgun, not attacker-controlled path), and redundant `update_plots()` calls during class-preset seeding (web has `_applying_class_preset` guard; desktop doesn't extend it to skip expensive work). Not settled (1 search). |
| `kayakgen/ui/web/app.py` | 2026-05-29 (search #2) | BUG-029 (low), BUG-030 (medium), BUG-031 (info, baseline), BUG-071 (low), BUG-072 (info, baseline) | Trame state binding, REST handlers, web layout. Tick 12 found the validity-badge / comparison-toggle / hydro-binding triad. Tick 24 re-search applied the 10-pattern lens systematically and found only one new low: BUG-071 STL-export REST endpoint accepts invalid `part` parameter without validation, geometry silently falls back to "deck". Strong positive baselines: REST handlers have structured error envelopes, CfdWebStore validates job_id (prevents path traversal), float comparisons use tolerance, NaN rendering is cosmetic only (cross-field validators upstream prevent propagation). Not settled (2 searches), but UI surfaces yield substantially less on re-search than eval/. |
| `kayakgen/ui/web/generate_*.py` | 2026-05-29 (search #1) | BUG-032 (low), BUG-033 (low) | Form-builder, frontier rendering, spec submission. Tick 13 confirmed strong positive baselines: workflow 0037's SUBMIT_BLOCKING_REASON_* constants enumerate all gates, kind-aware submit routes correctly, wire-payload integrity holds (build_spec_from_form_state byte-stable), objective-refusal logic is reactive, FORBIDDEN_METRIC_TOKENS filter is sound. Found only an infinity-handling edge case in frontier z-metric color ratios + an implicit-vs-explicit substring-match note on FORBIDDEN_METRIC_TOKENS. Not settled (1 search), but future passes likely redundant unless RFC 0057 / RFC 0044 changes land. |
| `kayakgen/ui/web/controllers.py` | 2026-05-29 (search #1) | BUG-034 (info, null) | Controller glue, export menu, state listeners. Tick 14 confirmed positive baseline: ~536 lines of glue; all 61 `__all__` exports resolve; exception handling consistent across async route handlers; `_applying_class_preset` guards state-listener re-entrancy; `stop_generate_state_listener()` cleans up on shutdown; `register_rest_routes` is idempotent. No actionable bugs. Not settled per the 3+-searches rule, but future passes likely redundant unless the module gains new state-management responsibility. |
| `kayakgen/ui/parameter_metadata.py` + `hydrostatics_metadata.py` | 2026-05-29 (search #1) | BUG-039 (low) | Registry shapes (RFC 0060/0062). Tick 16 confirmed strong positive baselines: `frozen=True` enforced, registry coverage matches consumers (7 hydro keys + 11 hull keys), no cross-registry overlap, units consistent (`m`, `m^2`, `kg`), no claim-state vocabulary leaks in labels/descriptions, no HTML/Markdown in descriptions, helpers stable + idempotent. Found only that `Field(min_length=1)` accepts whitespace-only strings (defense-in-depth gap; regression tests already catch the surface). Not settled (1 search), but future passes likely redundant for these small frozen registries. |

## Cool-down rule

A surface that was searched in the last 60 minutes is skipped
for the next tick. A surface that has been searched 3+ times
total with no new actionable findings drops to "settled" and is
not re-searched until either (a) `git log -- <surface>` shows
new commits since the last search, or (b) the operator manually
resets it to "never" in this table.

Update this file at the end of each tick. The next tick reads
the top of the un-cooled-down list and dispatches against it.
