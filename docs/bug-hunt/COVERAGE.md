# Bug-hunt surface coverage

| Surface | Last searched | Findings ids | Lookback notes |
|---|---|---|---|
| `kayakgen/cli/` | 2026-05-29 (search #1) | BUG-007 (high), BUG-008 (medium), BUG-009 (low), BUG-010 (low) | CLI subcommand argument validation, error-path coverage, exit codes. Tick 4 found path traversal in `stability promote-fixture` via unsanitised `fixture_id`, no validation on `serve --host/--port` (accidental 0.0.0.0 binds), unbounded `n_stations` causing OOM in `build-export`, and undocumented `tolerance_percent` bounds in `migrate-geometry`. Not settled (1 search). |
| `kayakgen/eval/calibration/` | 2026-05-29 (search #1) | BUG-011 (high), BUG-012 (high), BUG-013 (medium) | Fixture promotion validators, source-review packet acceptance, extractor edge cases. Tick 5 found empty-reasons gap on `ResistanceSourceReviewPacket` verdicts, path-traversal in `_validate_accepted_fit_ref_on_disk()` (third instance after BUG-005/BUG-007), and unchecked `non_promotion_reasons` token set. D025 relaxations themselves correctly implemented. Not settled (1 search). |
| `kayakgen/eval/cfd/` | 2026-05-29 (search #1) | BUG-003 (low), BUG-004 (medium) | Adapter dispatch, env-knob precedence, subprocess error handling, force.dat / case-template parsing. Tick 2 confirmed `shlex.quote` on subprocess args, `RawUnvalidatedClaimFields` enforcement, and three-mechanism opt-in precedence are correct; found a dead `_openfoam_succeeded_path_enabled()` and missing negative-case tests for env-var coercion. Not settled (1 search). |
| `kayakgen/eval/closed_volume/` | 2026-05-29 (search #1) | BUG-019 (medium), BUG-020 (medium), BUG-021 (high) | Geometry construction, self-intersection diagnostics. Tick 9 found float-equality on `bow_rake == 0.0` / `stern_rake == 0.0` (same shape as BUG-015), an unconditional `return False` in `dispatch_evidence_satisfies_profile()` making it dead code, and a critical cross-link to BUG-014: the closed-body builder silently accepts inverted Hull geometry (deck_height_m < draft_m) and masks it via face-winding flip. Not settled (1 search). |
| `kayakgen/eval/evidence/` | 2026-05-29 (search #1) | BUG-005 (high), BUG-006 (medium) | RFC 0045 binding gate enforcement, artifact sanitization, dispatch_state ordering. Tick 3 found path traversal in polymesh artifact validation and incorrect gate ordering (dispatch_state checked after hash validation). Not settled (1 search). |
| `kayakgen/eval/stability/` | 2026-05-29 (search #1) | BUG-001 (critical), BUG-002 (high) | `EMPTY_STABILITY_FIT_REGISTRY` consumption, accepted-fit lookup, GZ contracts. Tick 1 found RFC 0058 / stage-4 discriminator gap (`kind` field) and a missing constrained-trim validator on `StabilityFixturePromotionPacket`. Not settled. |
| `kayakgen/io/` | 2026-05-29 (search #1) | BUG-022 (medium), BUG-023 (medium), BUG-024 (low), BUG-025 (low) | File-format readers/writers; round-trip fidelity. Tick 10 found encoding gaps in Path.read_text() / Path.write_text() (no explicit UTF-8), missing NaN/Infinity validators on ResistanceCurve and Hydrostatics (contrast with GZCurve validators), non-atomic write pattern in save functions, and print statement in STL writer violating structured logging. Positive baselines: no path traversal, Pydantic NaN→null conversion works correctly, no archive handlers (zip-slip safe). Not settled (1 search). |
| `kayakgen/model/` | 2026-05-29 (search #1) | BUG-014 (critical), BUG-015 (medium), BUG-016 (low) | Pydantic model validators (Hull, claim records, advisory). Tick 6 found missing cross-field `deck_height_m >= draft_m` validator (geometry.py:207 silently inverts), float-equality rake check in `_validate_distribution_v2_coupling`, and undocumented `frozen=False` on Hull aggregate root. Parent thread verified all three claims against source before recording. Not settled (1 search). |
| `kayakgen/search/active/` | 2026-05-29 (search #1) | BUG-017 (low) | NSGA-II / EHVI math, seeded determinism, constraint refusal. Tick 7 confirmed strong positive baselines: seeded determinism end-to-end (no global RNG calls), NSGA-II tie-breaking deterministic, crowding-distance zero-div guarded, `ensure_objectives_claim_admissible_for_search` gate invoked at the right point, resume preserves the seed contract. Found only a low-severity `assert best_genome is not None` in the EHVI runner. Not settled (1 search), but future passes likely redundant unless RFC 0044/0047 changes land. |
| `kayakgen/search/` (non-active) | 2026-05-29 (search #1) | BUG-026 (high), BUG-027 (medium), BUG-028 (info, null) | Pareto, objective admissibility, sweep planner. Tick 11 found that `compare.py` calls `ensure_objectives_not_high_angle_gz` but never `ensure_objectives_claim_admissible_for_search` (RFC 0044 entry-point gate bypass — verified by grep) and `dominates()` in pareto.py uses bare `<`/`>` on floats (4th float-equality finding). Parent thread verified BUG-026 against source. Not settled (1 search). |
| `kayakgen/services/` | 2026-05-29 (search #1) | BUG-018 (info, null) | `evaluation.py` view-model helpers, identity / hash routes, design report. Tick 8 confirmed positive baselines on the 3 primary modules (`evaluation.py`, `identity.py`, `generative_jobs.py`): workflow 0037/0038/0039 widening end-to-end clean, `design_hash_for_hull` deterministic with `sort_keys=True`, `_spawn()` uses list-based argv with `start_new_session=True`. Lighter coverage on the other 12 modules; recommend a focused next pass there. Subprocess env-var inheritance noted as architectural fact (not a bug). Not settled (1 search). |
| `kayakgen/ui/desktop.py` | 2026-05-29 (search #1) | BUG-035 (medium), BUG-036 (low), BUG-037 (low, downgraded from medium/security), BUG-038 (low) | matplotlib slider wiring, PyQt event loop, deprecation shim. Tick 15 found class-preset narrowing missing a clamp guard (non-seeded sliders can retain out-of-range values), PyVista 3D-window cleanup gap, STL-export path not normalised (parent downgraded from subagent's medium/security — threat model is operator footgun, not attacker-controlled path), and redundant `update_plots()` calls during class-preset seeding (web has `_applying_class_preset` guard; desktop doesn't extend it to skip expensive work). Not settled (1 search). |
| `kayakgen/ui/web/app.py` | 2026-05-29 (search #1) | BUG-029 (low), BUG-030 (medium), BUG-031 (info, baseline) | Trame state binding, REST handlers, web layout. Tick 12 found NaN/inf rendering in `validity_badge_title_for()` (only triggers if Hull validation is bypassed; downgraded from subagent's "medium" after parent verification), comparison-source toggle leaking state across mode switches, and a positive-baseline note on the Hydro-tab description-binding pattern (safe today; would not be if registry stopped being hardcoded). Workflows 0037 and 0039 wiring otherwise sound. Not settled (1 search). |
| `kayakgen/ui/web/generate_*.py` | 2026-05-29 (search #1) | BUG-032 (low), BUG-033 (low) | Form-builder, frontier rendering, spec submission. Tick 13 confirmed strong positive baselines: workflow 0037's SUBMIT_BLOCKING_REASON_* constants enumerate all gates, kind-aware submit routes correctly, wire-payload integrity holds (build_spec_from_form_state byte-stable), objective-refusal logic is reactive, FORBIDDEN_METRIC_TOKENS filter is sound. Found only an infinity-handling edge case in frontier z-metric color ratios + an implicit-vs-explicit substring-match note on FORBIDDEN_METRIC_TOKENS. Not settled (1 search), but future passes likely redundant unless RFC 0057 / RFC 0044 changes land. |
| `kayakgen/ui/web/controllers.py` | 2026-05-29 (search #1) | BUG-034 (info, null) | Controller glue, export menu, state listeners. Tick 14 confirmed positive baseline: ~536 lines of glue; all 61 `__all__` exports resolve; exception handling consistent across async route handlers; `_applying_class_preset` guards state-listener re-entrancy; `stop_generate_state_listener()` cleans up on shutdown; `register_rest_routes` is idempotent. No actionable bugs. Not settled per the 3+-searches rule, but future passes likely redundant unless the module gains new state-management responsibility. |
| `kayakgen/ui/parameter_metadata.py` + `hydrostatics_metadata.py` | 2026-05-29 (search #1) | BUG-039 (low) | Registry shapes (RFC 0060/0062). Tick 16 confirmed strong positive baselines: `frozen=True` enforced, registry coverage matches consumers (7 hydro keys + 11 hull keys), no cross-registry overlap, units consistent (`m`, `m^2`, `kg`), no claim-state vocabulary leaks in labels/descriptions, no HTML/Markdown in descriptions, helpers stable + idempotent. Found only that `Field(min_length=1)` accepts whitespace-only strings (defense-in-depth gap; regression tests already catch the surface). Not settled (1 search), but future passes likely redundant for these small frozen registries. |

## Cool-down rule

A surface that was searched in the last 60 minutes is skipped
for the next tick. A surface that has been searched 3+ times
total with no new actionable findings drops to "settled" and is
not re-searched until either (a) `git log -- <surface>` shows
new commits since the last search, or (b) the operator manually
resets it to "never" in this table.

Update this file at the end of each tick. The next tick reads
the top of the un-cooled-down list and dispatches against it.
